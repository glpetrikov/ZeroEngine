# zerofetch

A fast and clean system fetch tool powered by syspeek.

## Install

```bash
cargo install zerofetch
```

Usage
```bash
zerofetch
zerofetch --verbose
zerofetch --json
zerofetch --no-logo
zerofetch --no-color
zerofetch --help
```
