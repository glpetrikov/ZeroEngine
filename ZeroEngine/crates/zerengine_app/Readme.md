# zerengine_app

Application layer crate for the ZeroEngine workspace.

This crate owns the high-level app lifecycle and integrates the engine with the window/event loop system.

It acts as the runtime bridge between platform events, input updates, engine systems, renderer updates, and application shutdown flow.
