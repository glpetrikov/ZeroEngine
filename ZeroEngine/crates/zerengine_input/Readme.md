# zerengine_input

Input handling crate for the ZeroEngine workspace.

This crate tracks keyboard and input state and provides a simple API for querying current, pressed, and released input events.

It is used by the application layer to update input state during the engine loop.
