# zerengine_log

Logging crate for the ZeroEngine workspace.

This crate initializes and exposes the engine logging API.

It provides a consistent logging layer for engine crates and the main executable.
