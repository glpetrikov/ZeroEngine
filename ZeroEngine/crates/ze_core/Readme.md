# ze_core

Core utilities crate for the ZeroEngine workspace.

This crate contains shared engine primitives, types, and low-level building blocks used across the engine.

It is intended to stay lightweight and independent from higher-level application or platform-specific logic.
